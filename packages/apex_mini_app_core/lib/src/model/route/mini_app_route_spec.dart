/// Describes one route that a mini-app module can build.
class MiniAppRouteSpec {
  /// Absolute mini-app route path.
  final String path;

  /// Creates a route spec for module route registration.
  MiniAppRouteSpec({required this.path});
}
